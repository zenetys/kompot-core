# Test: Host template with services
# Validates service declarations within host block

# Base template
host linux --template

# Template with services inheriting from linux
host webserver:linux --template
  service ADM-SSH
  service ADM-HTTPS
  service CPU

# Concrete host to trigger service generation
host myserver:webserver --address=10.0.0.1

# ASSERT service:myserver:ADM-SSH:check_command=check-dummy
# ASSERT service:myserver:ADM-HTTPS:check_command=check-dummy
# ASSERT service:myserver:CPU:check_command=check-dummy
