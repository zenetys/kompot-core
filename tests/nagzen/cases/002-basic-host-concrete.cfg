# Test: Basic concrete host declaration
# Validates host with address generates correct Nagios config

# Define the template first
host linux --template

# Concrete host using the template
host myserver:linux --address=192.168.1.10

# ASSERT host:myserver:address=192.168.1.10
