# Test: Host with custom variables
# Validates __CUSTOM variables on host level

# Base template
host linux --template

# Concrete host with custom variables
host myserver:linux --address=192.168.1.50 __TAGS=#production __ENVIRONMENT=prod

# ASSERT host:myserver:__TAGS=#production
# ASSERT host:myserver:__ENVIRONMENT=prod
