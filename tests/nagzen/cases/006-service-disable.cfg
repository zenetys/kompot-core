# Test: Service disable
# Validates --disable removes service from inheritance

host base:linux --template
  service ADM-SSH
  service CPU
  service MEM

host derived:base --template
  service CPU --disable
