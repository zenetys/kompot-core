# EXPECT_ERROR
# Test: Circular inheritance should be detected and rejected
#
# This test verifies that nagzen detects circular template inheritance
# and exits with a fatal error instead of hanging in an infinite loop.

host A:B --template
host B:A --template
