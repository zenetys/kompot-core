define command {
    command_name    nmap-syn
    command_line    $USER3$/check_nmap_syn \
                        --host '$HOSTADDRESS$' \
                        --port '$_SERVICE_PORT$' \
                        --warning '$_SERVICE_WARNING$' \
                        --critical '$_SERVICE_CRITICAL$' \
                        --sudo-user '$_SERVICE_SUDO_USER$'
}
